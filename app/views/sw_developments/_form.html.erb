<%= form_for(sw_development) do |f| %>
  <% if sw_development.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sw_development.errors.count, "error") %> prohibited this sw_development from being saved:</h2>

      <ul>
      <% sw_development.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
