<p id="notice"><%= notice %></p>

<h1>Sw Developments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sw_developments.each do |sw_development| %>
      <tr>
        <td><%= link_to 'Show', sw_development %></td>
        <td><%= link_to 'Edit', edit_sw_development_path(sw_development) %></td>
        <td><%= link_to 'Destroy', sw_development, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sw Development', new_sw_development_path %>
