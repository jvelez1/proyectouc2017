<p id="notice"><%= notice %></p>

<h1>Db Engies</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @db_engies.each do |db_engy| %>
      <tr>
        <td><%= link_to 'Show', db_engy %></td>
        <td><%= link_to 'Edit', edit_db_engy_path(db_engy) %></td>
        <td><%= link_to 'Destroy', db_engy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Db Engy', new_db_engy_path %>
